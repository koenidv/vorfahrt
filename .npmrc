#//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
#@koenidv:registry=https://npm.pkg.github.com
#node-linker=hoisted // fixme - react native won't work without hoisted, but pnpm deploy doesn't work with it